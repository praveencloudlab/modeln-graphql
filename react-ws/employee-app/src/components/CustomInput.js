import React from 'react';

const CustomInput = ({type,name}) => {
    return (
        <div>
           <input type={type} name={name}/> 
           
        </div>
    );
};

export default CustomInput;